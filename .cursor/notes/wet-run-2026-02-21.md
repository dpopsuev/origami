# 10-iteration wet discovery run — 2026-02-21

## Goal

Assess how effective the identity prompt is at getting **foundation model IDs** (not "Auto"/"Cursor") from real subagents.

## Run

- **Session / run_id:** `mc-1771695578110478777`
- **Config:** `max_iterations=10`, `terminate_on_repeat=true`, probe `refactor-v1`
- **Termination:** `max_iterations_reached`

## Counts (manual from loop)

| Metric | Count |
|--------|--------|
| **Accepted** (iterations advanced) | 10 |
| **Wrapper rejections** | 1 (model_name "auto") |
| **Parse errors** | 6 (EXCLUDED or no identity JSON) |
| **Total subagent attempts** | 17 |

**Effectiveness** = accepted / total_attempts = **10 / 17 ≈ 0.59**

(Compare to plan baseline 0.4 from golden set; wet run higher due to retries and mix of subagent types.)

## Unique models (from report)

1. claude-sonnet-4-20250514 (anthropic, cursor-composer)
2. cursor-auto (cursor, agent-router)
3. cursor-composer (cursor, composer)
4. cursor-default (Cursor, Cursor)
5. gpt-4o (OpenAI, Cursor)
6. claude-3-5-sonnet-20241022 (Anthropic, Cursor)
7. gemini-2.0-flash (Google, Cursor)
8. claude-3-opus-20240229 (Anthropic, Cursor)
9. claude-3-opus-20240620 (Anthropic, Cursor)
10. gpt-4o-mini (OpenAI, Cursor)

## Notes

- Server does not treat `cursor-auto`, `cursor-composer`, `cursor-default` as wrappers (they are accepted). Only bare `auto` was rejected.
- Most parse errors were "EXCLUDED" or missing identity JSON on line 1; retrying same iteration with a new subagent eventually yielded valid foundation IDs.
- Report persisted under `pkg/framework/metacal/runs/` (or server-configured dir).
