# Index

## Directories
- `bootstrap/` — create/verify .cursor structure (project-agnostic)
- `index-integrity/` — scan, validate, and enforce index.mdc compliance across .cursor/
- `onboard/` — teach newcomers how to work in the Origami repo
- `survey/` — scan repo and spill key concepts into .cursor

## Files
(No files)
