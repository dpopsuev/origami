apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge
  labels:
    app: origami-knowledge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: origami-knowledge
  template:
    metadata:
      labels:
        app: origami-knowledge
    spec:
      containers:
        - name: knowledge
          image: origami-knowledge:latest
          args: ["--port", "9100", "--drivers", "git,docs"]
          ports:
            - containerPort: 9100
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9100
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 9100
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge
spec:
  selector:
    app: origami-knowledge
  ports:
    - port: 9100
      targetPort: 9100
