name: ocp-platform
operator: OCP Platform
description: Shared OpenShift platform sources

repos:
  - org: openshift
    name: cluster-etcd-operator
    purpose: etcd lifecycle
    branch_pattern: "release-{ocp_version}"

docs:
  - docs/ocp-architecture.md
