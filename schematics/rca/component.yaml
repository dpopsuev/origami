component: asterisk-rca
namespace: rca
version: "1.0.0"
description: RCA circuit plumbing for CI root-cause analysis
provides:
  transformers: [context-builder, prompt-filler]
  extractors: [recall, triage, resolve, investigate, correlate, review, report]
  hooks: [store.recall, store.triage, store.investigate, store.correlate, store.review]
requires:
  origami: ">=0.3.0"
  sockets:
    - name: store
      type: store.Store
      description: Persistent storage for investigations, cases, and RCA results
    - name: source
      type: SourceAdapter
      description: Fetches test failures and pushes results back to the tracker